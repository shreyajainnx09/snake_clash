services:
  - type: web
    name: snake-clash
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn snake_clash:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
